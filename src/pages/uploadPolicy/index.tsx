import React from 'react';
import { Card } from 'antd';
import { PageContainer } from '@ant-design/pro-layout';

const UploadPolicy: React.FC = () => {
  return (
    <PageContainer>
      <Card>UploadPlicy</Card>
    </PageContainer>
  );
};

export default UploadPolicy;
